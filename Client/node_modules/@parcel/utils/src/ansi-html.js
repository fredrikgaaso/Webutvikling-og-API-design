// @flow strict-local
import ansiHTML from 'ansi-html-community';
import {escapeHTML} from '@parcel/utils/src/escape-html';

export function ansiHtml(ansi: string): string {
  return ansiHTML(escapeHTML(ansi));
}
